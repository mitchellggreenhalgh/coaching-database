from tkinter import *

class universal_format:
    frame_padding = '3 3 12 12'
    frame_sticky = (N, S, E, W)
    entry_sticky = (W, E)
    label_sticky = entry_sticky
    button_sticky = entry_sticky